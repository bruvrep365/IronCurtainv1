---
description: 'A iron-curtain app.'
labels: ['app', 'react']
---

A React application for iron-curtain. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use iron-curtain 
```

To run the app locally, run the following:

```
bit run iron-curtain 
```
